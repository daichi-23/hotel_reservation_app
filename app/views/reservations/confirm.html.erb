<div class="form-container">
  <h2>予約内容の確認</h2>

  <%= form_with model: @reservation, local: true do |f| %>
    <%= f.hidden_field :start_date %>
    <%= f.hidden_field :end_date %>
    <%= f.hidden_field :person_num %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :room_id, value: @room.id %>

    <div class="reservation-item">
      <p class="reservation-label">チェックイン</p>
      <p class="reservation-value"><%= @reservation.start_date.strftime("%Y/%m/%d") %></p>
    </div>
    <div class="reservation-item">
      <p class="reservation-label">チェックアウト</p>
      <p class="reservation-value"><%= @reservation.end_date.strftime("%Y/%m/%d") %></p>
    </div>
    <div class="reservation-item">
      <p class="reservation-label">日数</p>
      <p class="reservation-value"><%= @reservation.days_count %>日</p>
    </div>
    <div class="reservation-item">
      <p class="reservation-label">人数</p>
      <p class="reservation-value"><%= @reservation.person_num %>人</p>
    </div>
    <div class="reservation-item">
      <p class="reservation-label">お支払い金額</p>
      <p class="reservation-value"><%= @reservation.price_count.to_s(:delimited) %>円</p>
    </div>
    <p>上記内容でよろしければ「確定」ボタンを押してください。</p>
    <%= f.submit "予約を確定", class: "reservation-orange-button" %>

    <%= f.submit "キャンセル", name: :back,  class: "reservation-white-button" %>

  <% end %>


</div>